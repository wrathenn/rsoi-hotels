apiVersion: v1
kind: Service
metadata:
  name: stats
  labels:
    app.kubernetes.io/name: stats
spec:
  selector:
    app.kubernetes.io/name: stats
  ports:
    - port: 80
      name: http
      targetPort: http
