apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rsoi-hotels
  annotations:
    werf.io/weight: "-60"
spec:
  ingressClassName: nginx
  rules:
    - host: hotels.rsoi
      http:
        paths:
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  name: http
